# Deprecated in v3.0.1.
#
# The receiver-mode payment daemon and openai-worker-node now consume
# the same shared worker.yaml. Use worker.example.yaml instead and mount
# it into both containers at /etc/livepeer/worker.yaml.
